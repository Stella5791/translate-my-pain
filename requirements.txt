# Core functionality
flask             # Web app framework
nltk              # Stemming, tokenizing, linguistic processing
weasyprint        # HTML-to-PDF conversion

# Required for WeasyPrint to render properly
cairocffi         # Interface to Cairo graphics library
tinycss2          # CSS parser used by WeasyPrint
pyphen            # Hyphenation support

# Optional but helpful
python-dotenv     # For managing environment variables (e.g., FLASK_DEBUG, secrets)
